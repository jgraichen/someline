[project]
name = "someline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "~=3.12"
dependencies = [
  "build123d~=0.9.0",
  "click~=8.1.7",
  "fuzzysearch~=0.7.3",
]

[dependency-groups]
dev = [
  "ocp-vscode~=2.7.0",
  "pip>=25.0.1",
  "ty>=0.0.0a5",
]
